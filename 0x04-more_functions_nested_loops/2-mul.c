/**
 * mul  - a function that multiplies two integers.
 * @a: the first number is the multiplication
 * @b: the second number in the multiplcation
 *
 * Return: the multipcation of @a and @b
 */

int mul(int a, int b)
{
	return (a * b);
}
